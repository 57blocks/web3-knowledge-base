{
  "name": "57blocks-web3-knowledge-base",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "copy-files": "rimraf ./node_modules/npm/node_modules/graceful-fs",
    "patch:mermaid-gb3": "node scripts/patch-mermaid-gb3.js",
    "serve": "npm run patch:mermaid-gb3 && gitbook serve",
    "plugin": "gitbook install",
    "build": "npm run patch:mermaid-gb3 && gitbook build",
    "deploy": "npm run plugin && npm run build"
  },
  "dependencies": {
    "gitbook-cli": "2.1.2",
    "gitbook-plugin-pageview-count": "^1.0.1",
    "graceful-fs": "^4.2.10",
    "rimraf": "^4.4.0"
  }
}
